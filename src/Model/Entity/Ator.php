<?php
namespace App\Model\Entity;

use Cake\ORM\Entity;
use DateTime;

class Ator extends Entity
{
/*
    protected function _setNascimento($nascimento) {
        return date('Y-m-d', strtotime(str_replace('/', '-', $nascimento)));
    }

    protected function _getNascimento($nascimento) {
        return date('d/m/Y', strtotime($nascimento));
    }
*/    
}